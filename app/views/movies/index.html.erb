<table class="table table-hover">
  <tr>
    <th>Votes</th>
    <th>Name</th>
    <th>Upvote</th>
  </tr>
  <% @movies.each do |movie| %>
  <tr>
    <td> Votes: <%= movie.rank %></td>
    <td><%= link_to movie.name, movies_show_path(movie) %></td>
    <td><%= link_to "Upvote", movies_upvote_path(movie), class: "btn btn-default" %></td>
  </tr>
  <% end %>
</table>

<%= link_to "View All Media", root_path, class:"btn btn-default" %>
<%= link_to "Add a movie", movies_new_path, class:"btn btn-default" %>
