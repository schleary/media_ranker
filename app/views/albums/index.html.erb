<table class="table table-hover">
  <tr>
    <th>Ranking</th>
    <th>Name</th>
    <th>Upvote</th>
  </tr>
  <% @albums.each do |album| %>
  <tr>
    <td> Ranking: </td>
    <td><%= link_to album.name, albums_show_path(album) %></td>
    <td><%= link_to "Upvote", albums_show_path(album), class: "btn btn-default" %></td>
  </tr>
  <% end %>
</table>

<%= link_to "View All Media", root_path, class:"btn btn-default" %>
<%= link_to "Add an Album", albums_new_path, class:"btn btn-default" %>
